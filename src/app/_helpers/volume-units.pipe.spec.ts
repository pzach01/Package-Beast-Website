import { VolumeUnitsPipe } from './volume-units.pipe';

describe('VolumeUnitsPipe', () => {
  it('create an instance', () => {
    const pipe = new VolumeUnitsPipe();
    expect(pipe).toBeTruthy();
  });
});
