import { WeightUnitsPipe } from './weight-units.pipe';

describe('WeightUnitsPipe', () => {
  it('create an instance', () => {
    const pipe = new WeightUnitsPipe();
    expect(pipe).toBeTruthy();
  });
});
