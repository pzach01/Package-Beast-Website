import { SubscriptionChange } from './subscription-change';

describe('SubscriptionChange', () => {
  it('should create an instance', () => {
    expect(new SubscriptionChange()).toBeTruthy();
  });
});
