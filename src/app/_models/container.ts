export class Container {
    public height: String;
    public width: String;
    public depth: String;
    constructor(width, depth, height) {
        this.width = width;
        this.depth = depth;
        this.height = height;
      }
}
